package com.example;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringMaven {
public static void main(String[] args) {
	SpringApplication.run(SpringMaven.class, args);
	System.out.println("Ith ottakk ndaakkiya project using file new maven project then changing the pom.xml etc and \n downloading the required files and changing the parant class to springboot");
}
}
